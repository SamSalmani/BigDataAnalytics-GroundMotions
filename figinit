This code implements Konno-Ohmachi smoothing algorithm. It is faster than
% its previous version ("konnoOhmachiSmooth") for arrays longer than ~1500.
% This version can save as much as 40% of the time required for the 
% old version.
